
menuconfig ASMP
	bool "ASMP"
	help
		A hybrid multi processor framework

if ASMP
	config MM_TILE
	bool
	default y

	config ASMP_MEMSIZE
	hex "Memory size"
	default 0x80000
	help
		Memory size for shared memory area in ASMP Framework.

endif
